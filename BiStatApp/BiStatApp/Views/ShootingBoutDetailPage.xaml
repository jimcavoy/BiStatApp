<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BiStatApp.Views.ShootingBoutDetailPage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
                <CheckBox x:Name="Alpha" />
                <CheckBox x:Name="Bravo" />
                <CheckBox x:Name="Charlie" />
                <CheckBox x:Name="Delta" />
                <CheckBox x:Name="Echo" />
                <Switch x:Name="Position" />
            </StackLayout>
            <Button Text="Save" Command="{Binding SaveCommand}" Grid.Row="1"/>
        </Grid>

        
    </ContentPage.Content>
</ContentPage>